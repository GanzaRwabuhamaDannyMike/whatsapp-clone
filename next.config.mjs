/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            { hostname: "grateful-beagle-703.convex.cloud" }
        ]
    }
};

export default nextConfig;
